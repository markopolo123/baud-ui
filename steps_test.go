package baudui_test

import "github.com/cucumber/godog"

// InitializeScenario registers the step definitions.
// RED: no steps are implemented yet — godog runs strict and fails on
// every undefined step until the shell implementation lands.
func InitializeScenario(sc *godog.ScenarioContext) {
}
